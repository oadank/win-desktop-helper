你是被考核方。用桌面助手 MCP（win-desktop-helper，服务在 127.0.0.1:18800）把下面六项改造各真跑一次。
报告必须给出：工具名 + 你实际传的参数 + 返回里的关键字段值。不会用就写"不会用"，没跑通就写卡在哪。不许编成功，不许只说"已完成"。

1) ref 定位：用 ui_find 拿元素的 ref，再用 ref 完成一次点击，不许用坐标。报出 ref 值。
2) 落点校验：故意点一个被别的窗口盖住的位置，看工具是否报 blocked 拦下来。报出这次拦截的返回。
3) snap 分屏：用 win_manage action=snap 把一个窗口贴到半屏或角上。报出 pos 和最终 rect。
4) 分层手册：get_skill 分别用 topic=关键词 和 detail=full 各取一次。报出两次返回的长度差异。
5) tray_click：从任务栏或托盘唤一个应用。报出 name、double 参数和结果。
6) app_run：带 wait 和 process 启动一个程序。报出返回里的 stable 和 waitedMs。

反作弊：每一步都要真的调用工具，并贴出返回里的关键字段；凭记忆或推测写的答案不算；靠截图估坐标去点算违规。

# 存档说明（不属于原文，发给 ZCode 时删掉本行及以下）
# 2026-09-07 13:10 重建：旧版 _dsh_prompt.txt 里内嵌的原文被老大否（写成教材 + 让 zcode 自切模型），
# 本版为 v2 六项测试题：ref / 落点校验(含故意造失败) / snap / 分层手册 topic+detail / tray_click / app_run wait
# 切模型是 dsh 的活，不在 zcode 的题里；给 zcode 的只给任务+验收+反作弊，绝不给操作步骤
